var React = require("react");

var List = React.createClass({
    render: function () {
        return (
            <div id="List">
                <h1>List of posts</h1>
            </div>
        );
    }
});

module.exports = List;
