var React = require("react");

var Root = React.createClass({
    render: function () {
        return (
            <div id="Root">
                <h1> React </h1>
            </div>
        );
    }
});

module.exports = Root;
